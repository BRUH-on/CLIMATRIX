NODE_ENV=development
PORT=4000

# PostgreSQL connection (adjust for your local / docker-compose DB)
DATABASE_URL=postgresql://climacore:climacore@localhost:5432/climacore?schema=public

# Comma-separated list of allowed origins, or "*" for any
CORS_ORIGIN=http://localhost:5173

# pino log level: fatal | error | warn | info | debug | trace
LOG_LEVEL=info

# Phase 3 (auth) — must be set before Phase 3, leave blank for Phase 2
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
